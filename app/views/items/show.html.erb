<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @item.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @item.description %>
</p>

<p>
  <strong>Text:</strong>
  <%= @item.text %>
</p>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>
<%= link_to 'Edit', edit_store_item_path(@store, @item) %> |
<%= link_to 'Back to Store Page', store_items_path(@store) %>